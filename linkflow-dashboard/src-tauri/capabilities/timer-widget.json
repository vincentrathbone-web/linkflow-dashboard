{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "timer-widget",
  "description": "Permissions for the always-on-top floating timer widget window",
  "windows": [
    "timer-widget"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging"
  ]
}
